import * as React from "react";
import { Link } from "gatsby";
import Layout from "../components/layout";

const AboutPage = () => {
  return <Layout pageHeading="About" pageTitle="About Page"></Layout>;
};

export default AboutPage;
